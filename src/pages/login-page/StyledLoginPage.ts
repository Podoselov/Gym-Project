import { Box, styled } from '@mui/material';

export const StyledWraper = styled(Box)(() => ({
  width: '100%',
  height: '100%',
  margin: '0px',
  padding: '0px',
  backgroundColor: '#FFFFFF',
}));
